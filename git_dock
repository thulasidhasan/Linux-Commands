

git config --global user.name xxxx
git config --global user.email xxx@yyy.com

######## This commands for 1st time pushing our code #################3

 # mkdir script
 # cd script/
 # sudo vim echo.sh 
 # git init 
 # git add -A
 # git remote add origin https://github.com/thulasidhasan/shellscript.git
 # git push -u origin master

#########  make change in our code let push this #################

 #  sudo vim echo.sh 
 #  git add -A
 #  history 
 #  git status
 #  git commit -m 'added comment for test'
 #  git pull origin master
 #  git push origin master
 #  git status

